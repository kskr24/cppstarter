#include <bits/stdc++.h>

using namespace std;
void solve() {
    int n, k;
    cin>>n>>k;
    vector<vector<int>>dp(n+1, vector<int>(k+1));

    // base states
    // when you don't have any rectangles
    for(int i = 0; i <= k; k++){

    }

}

int main() {
    int t;
    cin >> t;
    while (t--) {
        solve();
    }
    return 0;
}
